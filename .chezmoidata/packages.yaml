packages:
  linux:
    arch:
      pacman:
        - zsh             # used instead of bash
        - zsh-completions
        - zoxide          # better cd
        - eza             # better ls
        - fzf             # a fuzzy finder
        - git             # plugins and dev
        - neovim          # better vi
        - kitty           # cool terminal
    ubuntu:
      cargo:
        - zoxide
